# 더치페이 + 참가 신청 흐름 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 비용 단어를 '더치페이'로, 참가 신청 버튼에 차감 안내를 넣고, 다이아 잔액에 따라 충전 화면/완료(홈) 분기를 토스트와 함께 처리한다.

**Architecture:** `Wallet`(고정 잔액) 단일 소스. 상세 화면이 잔액으로 분기(SnackBar + push/pop). 충전 화면은 플레이스홀더.

**Tech Stack:** Flutter 3.38 / Dart 3.10, `intl`, `flutter_test`.

**참조 스펙:** `docs/superpowers/specs/2026-05-21-join-flow-design.md`

---

## Task 1: 더치페이 + Wallet + 헤더 잔액

**Files:**
- Modify: `lib/models/meeting_cost.dart` + `test/meeting_cost_test.dart`
- Create: `lib/data/wallet.dart`
- Modify: `lib/features/home/widgets/home_header.dart` + `lib/features/home/home_screen.dart`

- [ ] **Step 1**: `test/meeting_cost_test.dart`에서 `'1/N'` → `'더치페이'`로 기대값 변경. `lib/models/meeting_cost.dart`에서 `split('1/N')` → `split('더치페이')`.

- [ ] **Step 2**: `lib/data/wallet.dart` 생성:
```dart
/// 내 다이아 잔액(목). 추후 백엔드/지갑으로 교체.
class Wallet {
  const Wallet._();
  static const int myDiamonds = 30;
}
```

- [ ] **Step 3**: `lib/features/home/widgets/home_header.dart`에 `diamonds` 추가:
  - 생성자: `const HomeHeader({super.key, required this.monthLabel, required this.diamonds});` + `final int diamonds;`
  - 다이아 칩의 `child: const Row(...)`를 `child: Row(...)`로 바꾸고 `Text('1,000', ...)`를 `Text('$diamonds', style: const TextStyle(fontSize: 13, fontWeight: FontWeight.w500, color: AppColors.textInfo))`로 변경. (Row 안 `Icon`/`SizedBox`는 `const` 유지)

- [ ] **Step 4**: `lib/features/home/home_screen.dart`:
  - import 추가: `import '../../data/wallet.dart';`
  - `HomeHeader(monthLabel: _monthLabel())` → `HomeHeader(monthLabel: _monthLabel(), diamonds: Wallet.myDiamonds)`.

- [ ] **Step 5**: `flutter test test/meeting_cost_test.dart && flutter analyze && flutter test`
Expected: 전부 PASS, No issues.

- [ ] **Step 6**: 커밋
```bash
git add -A && git commit -m "feat: 더치페이 label, Wallet source, header shows balance"
```
(커밋 본문 끝에: `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`)

---

## Task 2: 충전 화면 (`diamond_recharge_screen.dart`)

**Files:**
- Create: `lib/features/meeting/diamond_recharge_screen.dart`

- [ ] **Step 1**: 생성:
```dart
import 'package:flutter/material.dart';
import '../../theme/app_colors.dart';

/// 다이아 충전(플레이스홀더). 실제 결제는 범위 밖.
class DiamondRechargeScreen extends StatelessWidget {
  const DiamondRechargeScreen({super.key, required this.currentDiamonds});

  final int currentDiamonds;

  static const _options = [
    (50, 1100),
    (120, 2500),
    (300, 5900),
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.bgPrimary,
      appBar: AppBar(
        backgroundColor: AppColors.bgPrimary,
        title: const Text('다이아 충전'),
      ),
      body: ListView(
        padding: const EdgeInsets.all(20),
        children: [
          Container(
            padding: const EdgeInsets.all(16),
            decoration: BoxDecoration(
              color: AppColors.bgInfo,
              borderRadius: BorderRadius.circular(12),
            ),
            child: Row(
              children: [
                const Icon(Icons.diamond, size: 18, color: AppColors.textInfo),
                const SizedBox(width: 8),
                Text('보유 다이아 $currentDiamonds개',
                    style: const TextStyle(
                        fontSize: 14,
                        fontWeight: FontWeight.w600,
                        color: AppColors.textInfo)),
              ],
            ),
          ),
          const SizedBox(height: 20),
          const Text('충전 상품',
              style: TextStyle(fontSize: 14, fontWeight: FontWeight.w600)),
          const SizedBox(height: 10),
          for (final (amount, won) in _options)
            Container(
              margin: const EdgeInsets.only(bottom: 10),
              padding: const EdgeInsets.all(14),
              decoration: BoxDecoration(
                color: AppColors.bgPrimary,
                borderRadius: BorderRadius.circular(12),
                border: Border.all(color: AppColors.borderTertiary, width: 0.5),
              ),
              child: Row(
                children: [
                  const Icon(Icons.diamond, size: 18, color: AppColors.textInfo),
                  const SizedBox(width: 8),
                  Text('다이아 $amount개',
                      style: const TextStyle(
                          fontSize: 14, fontWeight: FontWeight.w500)),
                  const Spacer(),
                  Text('₩$won',
                      style: const TextStyle(
                          fontSize: 13, color: AppColors.textSecondary)),
                ],
              ),
            ),
        ],
      ),
    );
  }
}
```

- [ ] **Step 2**: `flutter analyze lib/features/meeting/diamond_recharge_screen.dart` → No issues.

- [ ] **Step 3**: 커밋
```bash
git add lib/features/meeting/diamond_recharge_screen.dart && git commit -m "feat: add DiamondRechargeScreen placeholder"
```
(커밋 본문 끝에 Co-Authored-By 줄 추가)

---

## Task 3: 참가 신청 버튼 + 분기 (`meeting_detail_screen.dart`)

**Files:**
- Modify: `lib/features/meeting/meeting_detail_screen.dart`
- Modify: `test/meeting_detail_screen_test.dart`

- [ ] **Step 1: 테스트 갱신/추가(실패 유도)**

`test/meeting_detail_screen_test.dart`를 전체 교체:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:intl/date_symbol_data_local.dart';
import 'package:moija/data/meeting_repository.dart';
import 'package:moija/features/meeting/diamond_recharge_screen.dart';
import 'package:moija/features/meeting/meeting_detail_screen.dart';

void main() {
  setUpAll(() async {
    await initializeDateFormatting('ko_KR');
  });

  testWidgets('renders meeting info, participants, and join CTA',
      (tester) async {
    final repo = MeetingRepository();
    final meeting = repo.allMeetings.firstWhere((m) => m.id == 't1');
    await tester.pumpWidget(MaterialApp(
      home: MeetingDetailScreen(meeting: meeting, repository: repo),
    ));
    await tester.pumpAndSettle();

    expect(find.text('퇴근 후 볼링'), findsOneWidget);
    expect(find.text('참가 신청하기'), findsOneWidget);
    expect(find.text('방장 수락 시 다이아 50개 차감'), findsOneWidget);
    expect(find.text('참가자 4명'), findsOneWidget);
    expect(find.text('HOST'), findsOneWidget);
  });

  testWidgets('low balance shows toast and opens recharge screen',
      (tester) async {
    final repo = MeetingRepository();
    final meeting = repo.allMeetings.firstWhere((m) => m.id == 't1');
    await tester.pumpWidget(MaterialApp(
      home: MeetingDetailScreen(
          meeting: meeting, repository: repo, diamonds: 30),
    ));
    await tester.pumpAndSettle();

    await tester.tap(find.text('참가 신청하기'));
    await tester.pumpAndSettle();

    expect(find.byType(DiamondRechargeScreen), findsOneWidget);
    expect(find.text('다이아 50개 이상일 때 참가 신청을 할 수 있어요'),
        findsOneWidget);
  });

  testWidgets('sufficient balance completes and returns home',
      (tester) async {
    final repo = MeetingRepository();
    final meeting = repo.allMeetings.firstWhere((m) => m.id == 't1');
    await tester.pumpWidget(MaterialApp(
      home: Builder(
        builder: (context) => Scaffold(
          body: Center(
            child: ElevatedButton(
              onPressed: () => Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (_) => MeetingDetailScreen(
                      meeting: meeting, repository: repo, diamonds: 1000),
                ),
              ),
              child: const Text('open'),
            ),
          ),
        ),
      ),
    ));

    await tester.tap(find.text('open'));
    await tester.pumpAndSettle();
    await tester.tap(find.text('참가 신청하기'));
    await tester.pumpAndSettle();

    expect(find.byType(MeetingDetailScreen), findsNothing);
    expect(find.text('참가 신청이 완료됐어요'), findsOneWidget);
  });
}
```

- [ ] **Step 2**: `flutter test test/meeting_detail_screen_test.dart` → FAIL.

- [ ] **Step 3: 상세 화면 수정**

`lib/features/meeting/meeting_detail_screen.dart`:

(a) import 추가:
```dart
import '../../data/wallet.dart';
import 'diamond_recharge_screen.dart';
```

(b) 생성자에 `diamonds` 추가:
```dart
  const MeetingDetailScreen({
    super.key,
    required this.meeting,
    required this.repository,
    this.diamonds = Wallet.myDiamonds,
  });

  final Meeting meeting;
  final MeetingRepository repository;
  final int diamonds;
```

(c) `bottomNavigationBar`의 `Column(...)` 자식(버튼 + 캡션 Row)을 다음 단일 버튼으로 교체(별도 캡션 Row 삭제):
```dart
          child: SizedBox(
            width: double.infinity,
            child: ElevatedButton(
              style: ElevatedButton.styleFrom(
                backgroundColor: AppColors.textPrimary,
                foregroundColor: AppColors.bgPrimary,
                padding: const EdgeInsets.symmetric(vertical: 12),
              ),
              onPressed: () => _onApply(context),
              child: const Column(
                mainAxisSize: MainAxisSize.min,
                children: [
                  Text('참가 신청하기',
                      style: TextStyle(
                          fontSize: 15, fontWeight: FontWeight.w600)),
                  SizedBox(height: 2),
                  Text('방장 수락 시 다이아 50개 차감',
                      style: TextStyle(fontSize: 11, color: Colors.white70)),
                ],
              ),
            ),
          ),
```
(기존 `Column(mainAxisSize: MainAxisSize.min, children: [SizedBox(...ElevatedButton...), SizedBox(height:6), Row(다이아 캡션)])` 전체를 위 `SizedBox(width: double.infinity, child: ElevatedButton(...))`로 대체)

(d) 클래스에 분기 메서드 추가:
```dart
  void _onApply(BuildContext context) {
    final messenger = ScaffoldMessenger.of(context);
    if (diamonds <= 50) {
      messenger.showSnackBar(const SnackBar(
          content: Text('다이아 50개 이상일 때 참가 신청을 할 수 있어요')));
      Navigator.push(
        context,
        MaterialPageRoute(
          builder: (_) => DiamondRechargeScreen(currentDiamonds: diamonds),
        ),
      );
    } else {
      messenger.showSnackBar(
          const SnackBar(content: Text('참가 신청이 완료됐어요')));
      Navigator.pop(context);
    }
  }
```

- [ ] **Step 4**: `flutter test test/meeting_detail_screen_test.dart` → PASS (3 tests).

- [ ] **Step 5**: `flutter analyze && flutter test` → No issues, 전부 PASS.

- [ ] **Step 6**: 커밋
```bash
git add lib/features/meeting/meeting_detail_screen.dart test/meeting_detail_screen_test.dart && git commit -m "feat: join flow with diamond gate (recharge / complete)"
```
(커밋 본문 끝에 Co-Authored-By 줄 추가)

---

## 최종 검증
- [ ] `flutter analyze` → No issues
- [ ] `flutter test` → 전부 PASS
- [ ] `flutter run`: 더치페이 표기, 버튼 안 차감 안내, 잔액 30이라 참가 신청 시 토스트 + 충전 화면 이동.
